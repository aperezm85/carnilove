---
const { id, title, text, className } = Astro.props;
---

<article
  id={id}
  class:list={[
    "flex flex-col gap-2 rounded-lg bg-text-beige p-5 max-w-[430px]",
    className,
  ]}
>
  <h2
    class="font-heading font-extrabold text-[34px]/[38px] uppercase text-text-primary"
  >
    {title}
  </h2>
  <p class="font-copy text-lg/[26px] text-text-primary">{text}</p>
</article>
